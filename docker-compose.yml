services:
  web:
    build: .
    image: wopr-systems-next:latest
    container_name: wopr-systems-next
    restart: unless-stopped
    ports:
      - "127.0.0.1:3413:3000"
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
